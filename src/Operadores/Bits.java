package Operadores;

public class Bits {
	// Estas son las operaciones de Bits
	public static void main(String[] args) {
		int num = 5;
		num = num << 1; // num = 10, equivale a num = num * 2
		num = num >> 1; // num = 5, equivale a num = num / 2
	}

}
